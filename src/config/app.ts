import type { AppConfig } from "@/types"

export const appConfig: AppConfig = {
  navItems: [
    {
      title: "Dashboard",
      href: "/dashboard",
    },
    {
      title: "Explore",
      href: "/explore",
    },
  ],
}
